# 📐 Citizen Portal 行動 APP 架構文件

> **版本**: 1.0.0
> **日期**: 2025-11-27
> **架構**: Vue 3 + Capacitor 跨平台架構

---

## 🏗️ 整體架構

```
┌─────────────────────────────────────────────────────────┐
│                   Citizen Portal APP                     │
│                  (一份程式碼,多平台部署)                     │
└─────────────────────────────────────────────────────────┘
                            │
            ┌───────────────┼───────────────┐
            │               │               │
     ┌──────▼──────┐ ┌─────▼─────┐ ┌──────▼──────┐
     │  Web App    │ │  iOS App  │ │ Android App │
     │  (瀏覽器)    │ │  (Xcode)  │ │ (AS/Gradle) │
     └─────────────┘ └───────────┘ └─────────────┘
```

---

## 🔄 技術棧層次架構

```
┌─────────────────────────────────────────────────────────┐
│ Layer 4: 業務邏輯層 (Business Logic)                      │
├─────────────────────────────────────────────────────────┤
│  - 市民服務功能                                           │
│  - 議題管理系統                                           │
│  - 意見反饋機制                                           │
│  - 使用者認證授權                                         │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│ Layer 3: 前端框架層 (Frontend Framework)                  │
├─────────────────────────────────────────────────────────┤
│  Vue 3.5.22                                             │
│  ├── Composition API                                     │
│  ├── Vue Router 4.6.3 (路由管理)                         │
│  ├── Pinia 3.0.3 (狀態管理)                              │
│  └── Element Plus 2.11.5 (UI 元件庫)                     │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│ Layer 2: 跨平台橋接層 (Cross-Platform Bridge)            │
├─────────────────────────────────────────────────────────┤
│  Capacitor 7.4.4                                        │
│  ├── Camera Plugin (相機功能)                            │
│  ├── Filesystem Plugin (檔案系統)                        │
│  ├── Splash Screen (啟動畫面)                            │
│  └── Native Bridge (原生橋接)                            │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│ Layer 1: 原生平台層 (Native Platform)                     │
├───────────────────────┬─────────────────────────────────┤
│  Android              │  iOS                            │
│  ├── Java/Kotlin      │  ├── Swift/Objective-C          │
│  ├── Android SDK 33+  │  ├── iOS SDK 15+                │
│  └── Gradle           │  └── CocoaPods                  │
└───────────────────────┴─────────────────────────────────┘
```

---

## 📱 Capacitor 工作原理

```
┌─────────────────────────────────────────────────────────┐
│                      Vue 3 Web App                       │
│  (運行在 WebView 中 - 如同瀏覽器)                          │
└─────────────────────────────────────────────────────────┘
                            │
                            │ JavaScript Bridge
                            ↓
┌─────────────────────────────────────────────────────────┐
│                   Capacitor Core                         │
│  (JavaScript ↔ Native 通訊層)                            │
└─────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ↓                   ↓                   ↓
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│ Camera Plugin │  │  File Plugin  │  │ Other Plugins │
│   (原生相機)   │  │  (檔案系統)    │  │   (GPS等)     │
└───────────────┘  └───────────────┘  └───────────────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            ↓
        ┌───────────────────────────────────┐
        │      Device Native APIs           │
        │  (Android/iOS 原生功能)            │
        └───────────────────────────────────┘
```

---

## 🎯 模組架構

### 前端模組結構

```
src/
├── 📱 App.vue (根元件)
│
├── 🎨 components/ (可重用元件)
│   ├── CameraUpload.vue       ✅ 相機上傳元件
│   ├── IssueCard.vue          (議題卡片)
│   ├── CommentList.vue        (意見列表)
│   └── UserAvatar.vue         (使用者頭像)
│
├── 📄 views/ (頁面元件)
│   ├── Home/                  (首頁)
│   ├── Issues/                (議題管理)
│   │   ├── List.vue
│   │   ├── Detail.vue
│   │   └── Create.vue
│   ├── Opinions/              (意見反饋)
│   ├── User/                  (使用者中心)
│   └── Example/
│       └── CameraDemo.vue     ✅ 相機示範
│
├── 🔧 utils/ (工具函式)
│   ├── camera.js              ✅ 相機工具
│   ├── request.js             (API 請求)
│   ├── auth.js                (認證工具)
│   └── validators.js          (驗證工具)
│
├── 🌐 api/ (API 介接)
│   ├── issue.js               (議題 API)
│   ├── opinion.js             (意見 API)
│   ├── user.js                (使用者 API)
│   └── upload.js              (上傳 API)
│
├── 📦 store/ (狀態管理)
│   ├── user.js                (使用者狀態)
│   ├── issue.js               (議題狀態)
│   └── app.js                 (應用狀態)
│
└── 🗺️ router/ (路由設定)
    └── index.js               (路由配置)
```

---

## 🔌 Plugin 架構

### 已整合 Plugins

```
@capacitor/camera
├── getPhoto()           拍照或選擇圖片
├── pickImages()         選擇多張圖片
├── checkPermissions()   檢查權限
└── requestPermissions() 請求權限

@capacitor/filesystem
├── writeFile()          寫入檔案
├── readFile()           讀取檔案
├── deleteFile()         刪除檔案
└── readdir()            列出目錄

@capacitor/splash-screen
├── show()               顯示啟動畫面
└── hide()               隱藏啟動畫面
```

### 可擴充 Plugins

```
未來可安裝:

@capacitor/geolocation
├── getCurrentPosition()  取得當前位置
└── watchPosition()       持續監控位置

@capacitor/push-notifications
├── register()            註冊推播
├── addListener()         監聽推播
└── removeAllDelivered()  清除通知

@capacitor/network
├── getStatus()           取得網路狀態
└── addListener()         監聽狀態變化

@capacitor/share
└── share()               分享內容

@capacitor/haptics
├── impact()              震動回饋
└── vibrate()             震動
```

---

## 📊 資料流架構

### 使用者上傳圖片流程

```
┌─────────────┐
│   使用者    │
│  點擊拍照   │
└──────┬──────┘
       │
       ↓
┌─────────────────────────────┐
│ CameraUpload.vue (元件)      │
│ - 觸發拍照事件               │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ camera.js (工具函式)          │
│ - takePicture()             │
│ - 請求權限                   │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ Capacitor Camera Plugin     │
│ - 呼叫原生相機 API           │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ Android/iOS 原生相機         │
│ - 拍照或選擇圖片             │
└──────┬──────────────────────┘
       │
       ↓ (回傳 Base64)
┌─────────────────────────────┐
│ camera.js                   │
│ - compressImage()           │
│ - 壓縮圖片                   │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ CameraUpload.vue            │
│ - emit('update:images')     │
│ - 更新圖片陣列               │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ 父元件 (例: Create.vue)      │
│ - 處理圖片上傳               │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ API Service (upload.js)     │
│ - 呼叫後端 API               │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ Backend Server              │
│ - 儲存圖片                   │
│ - 回傳圖片 URL               │
└─────────────────────────────┘
```

---

## 🔐 權限管理架構

### Android 權限流程

```
┌─────────────────────────────┐
│ APP 啟動                     │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ AndroidManifest.xml         │
│ - 宣告需要的權限             │
│   ✓ CAMERA                  │
│   ✓ READ_MEDIA_IMAGES       │
│   ✓ WRITE_EXTERNAL_STORAGE  │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ 使用者觸發相機功能           │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ checkCameraPermission()     │
│ - 檢查是否已授權             │
└──────┬──────────────────────┘
       │
       ├─ 已授權 → 直接使用相機
       │
       └─ 未授權 ↓
┌─────────────────────────────┐
│ requestCameraPermission()   │
│ - 彈出權限請求對話框         │
└──────┬──────────────────────┘
       │
       ├─ 使用者允許 → 使用相機
       │
       └─ 使用者拒絕 → 顯示錯誤訊息
```

### iOS 權限流程

```
┌─────────────────────────────┐
│ APP 啟動                     │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ Info.plist                  │
│ - NSCameraUsageDescription  │
│ - NSPhotoLibraryUsage...    │
└──────┬──────────────────────┘
       │
       ↓
(流程與 Android 類似)
```

---

## 🌐 網路架構

### API 通訊流程

```
┌─────────────────────────────┐
│ Vue Component               │
│ (例: Create.vue)             │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ API Service                 │
│ (例: api/issue.js)           │
│ - createIssue(data)         │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ Axios Instance              │
│ (utils/request.js)          │
│ - 設定 baseURL              │
│ - 加入 Token                │
│ - 錯誤處理                   │
└──────┬──────────────────────┘
       │
       ↓ HTTP Request
┌─────────────────────────────┐
│ Backend API                 │
│ (Java Spring Boot)          │
└──────┬──────────────────────┘
       │
       ↓ Response
┌─────────────────────────────┐
│ Component                   │
│ - 更新 UI                    │
│ - 顯示訊息                   │
└─────────────────────────────┘
```

---

## 📦 建置流程架構

### 開發模式

```
┌─────────────────────────────┐
│ npm run dev                 │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ Vite Dev Server             │
│ - 啟動開發伺服器             │
│ - Hot Module Replacement    │
│ - 即時編譯                   │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ 瀏覽器 (localhost:5173)      │
│ - 測試 Web 功能              │
└─────────────────────────────┘
```

### APP 建置流程

```
┌─────────────────────────────┐
│ npm run android/ios         │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ npm run build               │
│ - Vite 建置 Web 專案         │
│ - 輸出到 dist/               │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ npx cap sync                │
│ - 複製 dist/ 到原生專案      │
│ - 更新 Plugin 設定           │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ npx cap open android/ios    │
│ - 開啟原生 IDE               │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ Android Studio / Xcode      │
│ - Gradle/Xcode Build        │
│ - 產生 APK/IPA               │
└─────────────────────────────┘
```

---

## 🔄 狀態管理架構

### Pinia Store 結構

```
store/
├── user.js (使用者狀態)
│   ├── state
│   │   ├── token
│   │   ├── userInfo
│   │   └── isLoggedIn
│   ├── getters
│   │   └── isAdmin
│   └── actions
│       ├── login()
│       ├── logout()
│       └── updateProfile()
│
├── issue.js (議題狀態)
│   ├── state
│   │   ├── issues
│   │   ├── currentIssue
│   │   └── filters
│   ├── getters
│   │   └── filteredIssues
│   └── actions
│       ├── fetchIssues()
│       ├── createIssue()
│       └── updateIssue()
│
└── app.js (應用狀態)
    ├── state
    │   ├── platform
    │   ├── networkStatus
    │   └── loading
    └── actions
        └── checkNetwork()
```

---

## 🎨 UI 元件層次

```
App.vue (根元件)
│
├── Layout
│   ├── Header (頂部導航)
│   ├── Sidebar (側邊欄)
│   └── Footer (底部)
│
└── Router View
    │
    ├── Home (首頁)
    │   ├── Banner
    │   ├── IssueList
    │   └── Statistics
    │
    ├── Issues (議題)
    │   ├── List
    │   │   └── IssueCard (元件)
    │   ├── Detail
    │   │   ├── IssueInfo
    │   │   └── CommentList (元件)
    │   └── Create
    │       └── CameraUpload (元件) ✅
    │
    └── User (使用者)
        ├── Profile
        └── Settings
```

---

## 💾 本地儲存架構

### 儲存策略

```
┌─────────────────────────────┐
│ 使用者設定                   │
│ @capacitor/preferences       │
│ - 語言偏好                   │
│ - 主題設定                   │
│ - 通知設定                   │
└─────────────────────────────┘

┌─────────────────────────────┐
│ 認證資訊                     │
│ localStorage (Web)          │
│ SecureStorage (Native)      │
│ - JWT Token                 │
│ - Refresh Token             │
└─────────────────────────────┘

┌─────────────────────────────┐
│ 圖片快取                     │
│ @capacitor/filesystem        │
│ - 暫存拍攝的圖片             │
│ - 離線圖片儲存               │
└─────────────────────────────┘

┌─────────────────────────────┐
│ 資料快取                     │
│ IndexedDB (Web)             │
│ SQLite (Native - 可選)       │
│ - 離線資料                   │
│ - 草稿儲存                   │
└─────────────────────────────┘
```

---

## 🔒 安全架構

### 認證流程

```
┌─────────────────────────────┐
│ 使用者登入                   │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ POST /api/auth/login        │
│ { username, password }      │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ Backend 驗證                │
│ - 檢查帳號密碼               │
│ - 產生 JWT Token            │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ 儲存 Token                  │
│ - localStorage (Web)        │
│ - SecureStorage (Native)    │
└──────┬──────────────────────┘
       │
       ↓
┌─────────────────────────────┐
│ 後續 API 請求                │
│ - Headers: {                │
│     Authorization:          │
│     'Bearer <token>'        │
│   }                         │
└─────────────────────────────┘
```

---

## 📊 效能優化架構

### 圖片優化流程

```
原始圖片 (例: 4000x3000, 8MB)
        │
        ↓
┌─────────────────────────────┐
│ compressImage()             │
│ - 調整尺寸: 1920x1920        │
│ - 壓縮品質: 80%              │
└──────┬──────────────────────┘
        │
        ↓
壓縮後圖片 (1920x1440, ~500KB)
        │
        ↓
┌─────────────────────────────┐
│ 上傳到伺服器                 │
│ - 更快的上傳速度             │
│ - 節省流量                   │
└─────────────────────────────┘
```

### 路由懶加載

```javascript
// router/index.js
const routes = [
  {
    path: '/issues/create',
    component: () => import('@/views/Issues/Create.vue')
    // 只在訪問時才載入,減少初始載入時間
  }
]
```

---

## 🌍 多語言架構 (可選)

```
i18n/
├── zh-TW.json (繁體中文)
│   ├── common: { ... }
│   ├── issues: { ... }
│   └── user: { ... }
│
├── en-US.json (英文)
│   └── ...
│
└── index.js (i18n 設定)
```

---

## 📱 響應式設計架構

### 斷點設計

```
Mobile:   < 768px   (手機)
Tablet:   768-1024px (平板)
Desktop:  > 1024px  (桌面)

CSS 策略:
- Mobile First (優先設計手機版)
- Progressive Enhancement (漸進式增強)
```

### 元件自適應

```vue
<template>
  <div :class="['container', deviceClass]">
    <!-- 內容 -->
  </div>
</template>

<script setup>
import { computed } from 'vue';

const deviceClass = computed(() => {
  const width = window.innerWidth;
  if (width < 768) return 'mobile';
  if (width < 1024) return 'tablet';
  return 'desktop';
});
</script>

<style scoped>
.container.mobile { /* 手機樣式 */ }
.container.tablet { /* 平板樣式 */ }
.container.desktop { /* 桌面樣式 */ }
</style>
```

---

## 🎯 總結

此架構提供:
- ✅ 清晰的分層設計
- ✅ 模組化的程式碼組織
- ✅ 完整的資料流管理
- ✅ 安全的認證機制
- ✅ 優化的效能策略
- ✅ 可擴充的插件系統

**架構優勢:**
- 易於維護和擴充
- 程式碼複用性高
- 跨平台一致性
- 清晰的職責分離

---

**文件版本**: 1.0.0
**最後更新**: 2025-11-27
